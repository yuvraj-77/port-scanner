{
  "name": "advanced-port-scanner",
  "version": "1.0.0",
  "description": "Advanced cross-platform network security scanner",
  "main": "main.js",
  "scripts": {
    "start": "python main.py",
    "test": "python -m pytest tests/",
    "build": "python setup.py build"
  },
  "dependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.5"
  }
}